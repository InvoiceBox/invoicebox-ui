# Базовые показатели

Измерено вживую 2026-07-01 на `develop` @ `11c00f3`. По одному прогону на пункт (это не приложение со страницами, чтобы гонять Lighthouse N раз — см. примечание ниже); при перезамере после каждой волны рефакторинга используйте тот же метод, чтобы проверить регресс/улучшение.

## Lighthouse — N/A

`@invoicebox/ui` поставляет компоненты, а не страницы. Нет URL, на который можно направить Lighthouse, кроме сайта Storybook (`docs/`), а собственный перформанс Storybook не репрезентативен для приложения-потребителя, использующего эти компоненты. **Базовый замер Lighthouse не проводился.** Если реальный перформанс-бейзлайн понадобится позже, его нужно измерять внутри реального эталонного приложения-потребителя, а не в этом репозитории.

## Размер сборки (сборка `release.sh`, локальный прогон 2026-07-01)

| Артефакт | Размер |
|---|---|
| `dist/index.js` (точка входа CJS) | 12 КБ |
| `dist/index.js` gzip | 1.58 КБ |
| `dist/esm/` (все ESM-файлы суммарно) | 1.3 МБ |
| `docs/` (статический сайт Storybook) | 10 МБ |
| `node_modules/` (только для разработки, не поставляется) | 653 МБ |

Примечание: 1.3 МБ для `dist/esm/` — это **сумма всех компонентов**, а не то, что попадает в бандл потребителя — `sideEffects: false` + отдельные ESM-файлы на компонент означают, что реальный бандлер потребителя подтягивает только реально импортированные компоненты. Крупнейшие отдельные собранные файлы: `AppCardLink/components/GooglePlayRu.js` (24 КБ, инлайновая SVG-иконка), `Select/index.js` (16 КБ), `Autocomplete/index.js` (16 КБ). Анализатор бандла (например, `source-map-explorer`, `webpack-bundle-analyzer`) не запускался — эти цифры получены через `du`, а не через реальный разбор графа зависимостей. Считайте это грубым бейзлайном, а не диагнозом.

## Тесты

- **Test suites:** 3 (но это *один и тот же* файл, обнаруженный 3 раза — см. ловушку с конфигом jest в [00-PROJECT-MAP.md](00-PROJECT-MAP.md))
- **Уникальные тестовые файлы:** 1 (`src/validate/utils/websiteValidate.test.ts`)
- **Тест-кейсы:** 6
- **Время выполнения:** 0.8с
- **Покрытие:** не измерялось (запуск с `--coverage` не настроен); учитывая 1 тестовый файл на ~41 компонент + 6 хуков, реальное покрытие логики компонентов ~0%.

## TypeScript

`npx tsc --project tsconfig.json` (реально релевантный для сборки вызов) — **0 ошибок**, чистая компиляция. (Отдельный `tsc --noEmit` падает с ошибкой, потому что `tsconfig.json` включает `emitDeclarationOnly: true`, а TypeScript отказывается сочетать это с `--noEmit` — это особенность вызова инструмента, а не ошибка типов в коде.)

## `npm audit` (2026-07-01, база уязвимостей самого npm)

| Серьёзность | Количество |
|---|---|
| Critical | 3 |
| High | 31 |
| Moderate | 38 |
| Low | 13 |
| **Всего** | **85** |

Топ по серьёзности: `form-data`, `handlebars`, `shell-quote` (все critical, все транзитивные — приходят через dev-цепочку Storybook/Babel/webpack, а не через 5 runtime-зависимостей). Среди прямых devDependencies тоже есть находки: `storybook` (high, несколько advisory через `ws`/`tar-fs`/`underscore`/`uuid`), `@babel/core` (low — произвольное чтение файла через комментарий sourcemap, GHSA-4x5r-pxfx-6jf8). Ни одна из 85 не относится к 5 пакетам, реально поставляемым потребителям (`@mona-health/react-input-mask`, `qrcode.react`, `react-calendar`, `react-currency-input-field`, `react-toastify`) — полная картина в [07-platform.md](07-platform.md).

## Актуальность зависимостей (`npm outdated`, 2026-07-01)

Каждая крупная devDependency отстаёт от актуальной версии на несколько мажоров:

| Пакет | Текущая | Актуальная |
|---|---|---|
| Storybook (`@storybook/*`) | 7.6.20 | 8.6–10.4 (зависит от пакета) |
| ESLint | 8.57.1 | 10.6.0 |
| `@typescript-eslint/*` | 5.62.0 | 8.62.1 |
| TypeScript | 4.9.5 | (5.x+ актуальная не установлена как devDep для сравнения, но 4.9 близка к EOL) |
| React (peer, установлен для разработки) | 18.3.1 | 19.2.7 |
| `@babel/*` core-пакеты | 7.25.x | 7.29.x (мажоры 8.0 тоже вышли) |

Полная разбивка в [07-platform.md](07-platform.md).

## Трекер ошибок

N/A — у библиотеки нет собственной runtime-телеметрии (подтверждено: нет Sentry/Bugsnag/analytics SDK нигде в `src/`). Нет трекера, из которого можно вытащить базовый список ошибок. См. [09-observability.md](09-observability.md) о том, как должен выглядеть observability-контракт для потребителя, если библиотека когда-нибудь добавит собственные хуки для репортинга ошибок.

## Методология

Все цифры выше получены живыми командами на `develop` @ `11c00f3` 2026-07-01 (`npm test`, `npx tsc`, `npm audit --json`, `npm outdated`, `du -sh`, `gzip -c | wc -c`) — по одному прогону на пункт, а не медиана из N, поскольку это детерминированные результаты сборки/статического анализа, а не чувствительные ко времени runtime-метрики. Если при будущих замерах появятся чувствительные ко времени метрики (например, время сборки Storybook, реальный Lighthouse в приложении-потребителе), переходить на медиану из 5 прогонов и фиксировать версию инструмента + build id рядом с результатом.
