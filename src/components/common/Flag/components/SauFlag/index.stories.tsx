import type { Meta, StoryObj } from '@storybook/react';
import { SauFlag } from '.';

const meta: Meta<typeof SauFlag> = {
    title: 'common/Flag/_components/SauFlag',
    component: SauFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof SauFlag> = {};
