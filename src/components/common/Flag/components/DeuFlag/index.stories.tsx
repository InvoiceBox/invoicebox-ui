import type { Meta, StoryObj } from '@storybook/react';
import { DeuFlag } from '.';

const meta: Meta<typeof DeuFlag> = {
    title: 'common/Flag/_components/DeuFlag',
    component: DeuFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof DeuFlag> = {};
