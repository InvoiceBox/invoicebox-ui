import type { Meta, StoryObj } from '@storybook/react';
import { AzeFlag } from '.';

const meta: Meta<typeof AzeFlag> = {
    title: 'common/Flag/_components/AzeFlag',
    component: AzeFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof AzeFlag> = {};
