import type { Meta, StoryObj } from '@storybook/react';
import { AusFlag } from '.';

const meta: Meta<typeof AusFlag> = {
    title: 'common/Flag/_components/AusFlag',
    component: AusFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof AusFlag> = {};
