import type { Meta, StoryObj } from '@storybook/react';
import { IndFlag } from '.';

const meta: Meta<typeof IndFlag> = {
    title: 'common/Flag/_components/IndFlag',
    component: IndFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof IndFlag> = {};
