import type { Meta, StoryObj } from '@storybook/react';
import { AreFlag } from '.';

const meta: Meta<typeof AreFlag> = {
    title: 'common/Flag/_components/AreFlag',
    component: AreFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof AreFlag> = {};
