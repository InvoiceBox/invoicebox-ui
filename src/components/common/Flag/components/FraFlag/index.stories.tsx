import type { Meta, StoryObj } from '@storybook/react';
import { FraFlag } from '.';

const meta: Meta<typeof FraFlag> = {
    title: 'common/Flag/_components/FraFlag',
    component: FraFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof FraFlag> = {};
