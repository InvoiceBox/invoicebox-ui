import type { Meta, StoryObj } from '@storybook/react';
import { BlrFlag } from '.';

const meta: Meta<typeof BlrFlag> = {
    title: 'common/Flag/_components/BlrFlag',
    component: BlrFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof BlrFlag> = {};
