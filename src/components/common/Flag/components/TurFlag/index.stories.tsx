import type { Meta, StoryObj } from '@storybook/react';
import { TurFlag } from '.';

const meta: Meta<typeof TurFlag> = {
    title: 'common/Flag/_components/TurFlag',
    component: TurFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof TurFlag> = {};
