import type { Meta, StoryObj } from '@storybook/react';
import { UzbFlag } from '.';

const meta: Meta<typeof UzbFlag> = {
    title: 'common/Flag/_components/UzbFlag',
    component: UzbFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof UzbFlag> = {};
