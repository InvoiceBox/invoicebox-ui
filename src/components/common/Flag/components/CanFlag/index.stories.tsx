import type { Meta, StoryObj } from '@storybook/react';
import { CanFlag } from '.';

const meta: Meta<typeof CanFlag> = {
    title: 'common/Flag/_components/CanFlag',
    component: CanFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof CanFlag> = {};
