import type { Meta, StoryObj } from '@storybook/react';
import { SrbFlag } from '.';

const meta: Meta<typeof SrbFlag> = {
    title: 'common/Flag/_components/SrbFlag',
    component: SrbFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof SrbFlag> = {};
