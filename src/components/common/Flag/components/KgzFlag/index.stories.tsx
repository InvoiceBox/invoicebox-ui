import type { Meta, StoryObj } from '@storybook/react';
import { KgzFlag } from '.';

const meta: Meta<typeof KgzFlag> = {
    title: 'common/Flag/_components/KgzFlag',
    component: KgzFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof KgzFlag> = {};
