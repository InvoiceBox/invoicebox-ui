import type { Meta, StoryObj } from '@storybook/react';
import { ArmFlag } from '.';

const meta: Meta<typeof ArmFlag> = {
    title: 'common/Flag/_components/ArmFlag',
    component: ArmFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof ArmFlag> = {};
