import type { Meta, StoryObj } from '@storybook/react';
import { RusFlag } from '.';

const meta: Meta<typeof RusFlag> = {
    title: 'common/Flag/_components/RusFlag',
    component: RusFlag,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof RusFlag> = {};
