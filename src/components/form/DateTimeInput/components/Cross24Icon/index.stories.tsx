import type { Meta, StoryObj } from '@storybook/react';
import { Cross24Icon } from './index';

const meta: Meta<typeof Cross24Icon> = {
    title: 'form/DateTimeInput/_components/Cross24Icon',
    component: Cross24Icon,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof Cross24Icon> = {};
