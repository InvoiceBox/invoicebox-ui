import type { Meta, StoryObj } from '@storybook/react';
import { CrossIcon } from './index';

const meta: Meta<typeof CrossIcon> = {
    title: 'form/DateTimeInput/_components/CrossIcon',
    component: CrossIcon,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof CrossIcon> = {};
