import type { Meta, StoryObj } from '@storybook/react';
import { Icon } from '.';

const meta: Meta<typeof Icon> = {
    title: 'form/DateInput/_components/Icon',
    component: Icon,
    tags: ['autodocs'],
};

export default meta;

export const Default: StoryObj<typeof Icon> = {};
